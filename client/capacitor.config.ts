import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.blackfish.client',
  appName: 'blackfish',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
